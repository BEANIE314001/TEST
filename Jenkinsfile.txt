pipeline {

  environment {
    dockerimagename = "duchaido/nodeapp"
    dockerImage = ""
  }

  agent any

    stage('Deploying App to Kubernetes') {
      steps {
        script {
          kubernetesDeploy(configs: "deploymentservice.yml", kubeconfigId: "kubernetes")
        }
      }
    }

  }

}
